package com.chargingthefuture.chyme.ui.theme

import androidx.compose.ui.graphics.Color

// Light theme colors - High contrast and accessible
val LightPrimary = Color(0xFF4ADE80) // Primary green brand color
val LightOnPrimary = Color(0xFF000000) // Black text on bright green for high contrast (WCAG AAA)
val LightSecondary = Color(0xFF1E40AF) // Deep blue for secondary actions
val LightOnSecondary = Color(0xFFFFFFFF) // White text on deep blue
val LightBackground = Color(0xFFFFFFFF) // Pure white for maximum contrast
val LightOnBackground = Color(0xFF000000) // Pure black text for maximum contrast
val LightSurface = Color(0xFFFFFFFF) // Pure white surface
val LightOnSurface = Color(0xFF000000) // Pure black text on surface
val LightError = Color(0xFFDC2626) // High contrast red for errors
val LightOnError = Color(0xFFFFFFFF) // White text on error

// Dark theme colors - High contrast and accessible
val DarkPrimary = Color(0xFF4ADE80) // Same bright green for consistency
val DarkOnPrimary = Color(0xFF000000) // Black text on bright green for high contrast
val DarkSecondary = Color(0xFF60A5FA) // Bright blue for secondary in dark theme
val DarkOnSecondary = Color(0xFF000000) // Black text on bright blue
val DarkBackground = Color(0xFF000000) // Pure black for maximum contrast
val DarkOnBackground = Color(0xFFFFFFFF) // Pure white text for maximum contrast
val DarkSurface = Color(0xFF1A1A1A) // Very dark gray surface (slightly lighter than black)
val DarkOnSurface = Color(0xFFFFFFFF) // Pure white text on surface
val DarkError = Color(0xFFEF4444) // Bright red for errors in dark theme
val DarkOnError = Color(0xFFFFFFFF) // White text on error

// Common colors - High contrast variants
val AccentGreen = Color(0xFF4ADE80) // Match primary green
val AccentRed = Color(0xFFDC2626) // High contrast red
val AccentYellow = Color(0xFFF59E0B) // High contrast amber/yellow
val Gray900 = Color(0xFF000000) // Pure black
val Gray700 = Color(0xFF1A1A1A) // Very dark gray
val Gray500 = Color(0xFF6B7280) // Medium gray for disabled states
val Gray300 = Color(0xFFD1D5DB) // Light gray for borders
val Gray100 = Color(0xFFF3F4F6) // Very light gray

